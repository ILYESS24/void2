name = "void-code-pages"
compatibility_date = "2024-11-03"

# Configuration Cloudflare Pages
pages_build_output_dir = "dist"

[build]
command = "npm run build:cloudflare"
cwd = "."

[site]
bucket = "./dist"

# Pas besoin de KV pour Pages - les fichiers sont servis directement depuis le bucket

